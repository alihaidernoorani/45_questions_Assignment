"use strict";
let famous_person = "Albert Einstein"; //Store the famous person's name in personName
let message = `${famous_person} once said, “A person who never made a mistake never tried anything new."`; //Composing message
console.log(message); //Prints the message
