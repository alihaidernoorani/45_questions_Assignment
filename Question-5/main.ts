let personName: string = "Albert Einstein"; //Store the famous person's name in personName
let message: string = `${personName} once said, “A person who never made a mistake never tried anything new."`; //Composing message
console.log(message); //Prints the message